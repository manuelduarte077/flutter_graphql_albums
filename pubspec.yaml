name: flutter_graphql_albums
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  graphql_flutter: ^5.1.1-beta.5
  graphql: ^5.1.2-beta.6
  graphql_codegen: ^0.11.2


dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
